[[feature]]
id = "library.network"
kind = "library_module"
syntax = "import network"
member_namespace = "library.network"
members = [{ id = "library.network.network_error_type", syntax = "NetworkError", universal = "network::NetworkError class" }, "Ip", "Address", "new_error", "network_error", "network_failure", "stream_error", "valid_port", "native_handle", "bytes_from", "address_from", "parse_ip", "TcpConnection", "TcpListener", "Datagram", "UdpConnection", "connect", "listen", "connect_text", "listen_text", "accept", "read_text", "write_text", "shutdown_write", "close", "exchange_text", "resolve", "udp_bind", "loopback_echo", "echo_once"]
export_sources = ["library/network/src/lib.sev"]
export_symbols = ["NetworkError", "Ip", "Address", "new_error", "network_error", "network_failure", "stream_error", "valid_port", "native_handle", "bytes_from", "address_from", "parse_ip", "TcpConnection", "TcpListener", "Datagram", "UdpConnection", "connect", "listen", "connect_text", "listen_text", "accept", "read_text", "write_text", "shutdown_write", "close", "exchange_text", "resolve", "udp_bind", "loopback_echo", "echo_once"]
type_params = []
parameters = ["address, connection, payload, timeout, or socket depending on the operation"]
constraints = ["ports are valid and resource handles are live"]
returns = "typed network value or NetworkError union"
effects = ["network", "io"]
errors = ["NetworkError", "StreamError"]
ownership = "Connection/listener/socket classes own native handles and close them explicitly."
universal = "network package public export"
lowering = "typed Severian wrapper over versioned native networking ABI"
status = "partial"
since = "2026"
tests = ["library/network"]
examples = ["library/network/docs/examples"]
limitations = ["Helper functions are currently exported because per-declaration visibility is not yet implemented."]
snippets = ["library.network.smoke"]

[[snippet]]
id = "library.network.smoke"
covers = ["library.network"]
source = '''
import network

def valid_loopback_port(port: int) -> bool:
    return network.valid_port(port)
'''
