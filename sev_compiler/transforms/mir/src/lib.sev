import artifact
import universal
import "cfg.sev"
import "model/operation/mod.sev"
import "model/value/mod.sev"
import "ownership.sev"
import "passes.sev"
import "verify.sev"
import "build/mod.sev"

def empty_module() -> universal.Module:
    result = universal.empty_module()
    result.initializer_cfg = universal.empty_cfg(universal.TypeId(0))
    return result
