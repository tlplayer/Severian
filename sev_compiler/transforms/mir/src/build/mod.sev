import universal
import diagnostics
import * from "../callable.sev" as callable


# Both driver entries lower through the same executable CFG builder.
def build(program: universal.Program) -> universal.Module | diagnostics.Diagnostic:
    result := universal.empty_module()
    for module in program.modules:
        result = callable.build_callables(module)
    return result
