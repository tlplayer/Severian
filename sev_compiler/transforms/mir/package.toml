[package]
name = "sev-compiler-mir"
version = "0.1.0"
edition = "2026"
publish = false

[lib]
path = "src/lib.sev"

[dependencies]
artifact = { path = "../../artifact" }
universal = { path = "../../universal" }
