[package]
name = "sev-compiler-universal"
version = "0.1.0"
edition = "2026"
publish = false

[lib]
path = "src/lib.sev"

[dependencies]
profiling = { path = "../../library/system/profiling" }
source = { path = "../source" }
diagnostics = { path = "../diagnostics" }
