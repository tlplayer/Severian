[package]
name = "sev-compiler-cfg"
version = "0.1.0"
edition = "2026"
publish = false

[lib]
path = "cfg.sev"

[dependencies]
source = { path = "../../source" }
diagnostics = { path = "../../diagnostics" }
