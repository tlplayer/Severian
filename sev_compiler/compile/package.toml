[package]
name = "sev-compiler-compile"
version = "0.1.0"
edition = "2026"
publish = false

[lib]
path = "src/lib.sev"

[dependencies]
artifact = { path = "../artifact" }
mir = { path = "../transforms/mir" }
mlir = { path = "../transforms/mlir" }
target = { path = "../target" }
universal = { path = "../universal" }
