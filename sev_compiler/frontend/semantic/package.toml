[package]
name = "sev-compiler-semantic"
version = "0.1.0"
edition = "2026"
publish = false

[lib]
path = "src/lib.sev"

[dependencies]
diagnostics = { path = "../../diagnostics" }
modules = { path = "../modules" }
source = { path = "../../source" }
universal = { path = "../../universal" }
