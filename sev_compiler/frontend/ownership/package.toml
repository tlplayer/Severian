[package]
name = "sev-compiler-ownership"
version = "0.1.0"
edition = "2026"
publish = false

[lib]
path = "src/lib.sev"

[dependencies]
diagnostics = { path = "../../diagnostics" }
universal = { path = "../../universal" }
