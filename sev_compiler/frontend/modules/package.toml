[package]
name = "sev-compiler-modules"
version = "0.1.0"
edition = "2026"
publish = false

[lib]
path = "src/lib.sev"

[dependencies]
ast = { path = "../ast" }
diagnostics = { path = "../../diagnostics" }
lexer = { path = "../lexer" }
parser = { path = "../parser" }
source = { path = "../../source" }
