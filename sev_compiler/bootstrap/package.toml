[package]
name = "sev-compiler-bootstrap"
version = "0.1.0"
edition = "2026"
publish = false

[lib]
path = "src/lib.sev"

[dependencies]
compile_protocol = { path = "../../library/core/compile" }
ast = { path = "../frontend/ast" }
diagnostics = { path = "../diagnostics" }
lexer = { path = "../frontend/lexer" }
parser = { path = "../frontend/parser" }
primitives = { path = "../../library/core/primitives" }
source = { path = "../source" }
universal = { path = "../universal" }
