[package]
name = "sev-compiler"
version = "0.1.0"
edition = "2026"
publish = false
default-run = "sev-bootstrap"

[[bin]]
name = "sev-bootstrap"
path = "src/main.sev"

[dependencies]
driver = { path = "boundaries/driver" }
interface = { path = "boundaries/interface" }
