def fetch(url: string) -> Result[string, IOError]:
    wait(0.1s)
    return true

test with profile with 
{
    0.1s < time -> exception("Await didn't actually wait ")
}:
    task = async fetch("https://example.test") with self
    result = await task
    assert(result)