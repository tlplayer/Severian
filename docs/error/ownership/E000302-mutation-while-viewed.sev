# E000302: mutation while an immutable view is live
# Structural mutation could invalidate the live view.
# Finish using the view first, or clone when an independent snapshot is needed.

def main():
    values := [1, 2, 3]
    values_view = view values
    values.push(4)
    print(values_view[0])
