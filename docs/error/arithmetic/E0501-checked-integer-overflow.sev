# E0501: checked integer overflow
# Safe integer arithmetic cannot exceed the destination type.
# Select wrapping_add, saturating_add, or overflowing_add explicitly when that
# behavior is part of the algorithm.

def main():
    byte: u8 = 255
    print(byte + 1)
