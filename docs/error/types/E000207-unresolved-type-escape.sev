# E000207: unresolved T escaped semantic analysis
# Strict packages reject an untyped parameter before MIR construction.
# Add a concrete annotation, or write `Any` explicitly when dynamic typing is
# intentional.

def double(value) -> int:
    return value * 2
