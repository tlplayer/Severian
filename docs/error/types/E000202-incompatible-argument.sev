# E000202: incompatible argument T
# Calls must satisfy the declared parameter T.
# Convert the value or correct the function signature.

def double(value: int) -> int:
    return value * 2

def main():
    print(double("two"))
