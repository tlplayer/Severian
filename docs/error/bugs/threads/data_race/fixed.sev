class Counter:
    v: int = 0

    def increment():
        v += 1

def main():
    counter := Counter()
    left = async counter.increment() with self and lock
    right = async counter.increment() with self and lock

    await right, left
    assert(counter.v == 2)
