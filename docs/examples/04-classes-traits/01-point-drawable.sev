trait Drawable:
    draw()

class Point: Drawable
    x: float
    y: float

    def magnitude() -> float:
        return sqrt(x*x + y*y)

    def draw():
        print("point", x, y)

def main():
    p = Point(3.0, 4.0)
    p.draw()
    print(p.magnitude())
