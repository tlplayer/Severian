trait Number:
    zero() -> Self
    add(other: Self) -> Self

def sum_values[key: Hash + Equal, value: Number](values: map[key, value]) -> value:
    total := value.zero()

    for _, value in values:
        total = total.add(value)

    return total

def main():
    counts = {"first": 34, "second": 8}
    print(sum_values(counts))
