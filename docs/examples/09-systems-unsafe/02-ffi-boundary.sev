def c_strlen() -> string:
    return 
    """
// strlen_wrapper.c
#include <stddef.h>
#include <string.h>

size_t sev_strlen(const char *text)
{
    return strlen(text);
}
    """

def strlen(text: string) -> usize:
    return c_strlen(text)

def main():
    print(strlen("severian"))
