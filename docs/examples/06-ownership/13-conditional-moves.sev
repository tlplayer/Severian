test with compiler:
    reject:
        value := "hello"
        condition := true

        if condition:
            consumed := move value

        print(value)

test:
    value := "hello"

    if true:
        print(borrow value)
    else:
        print(borrow value)

    print(value)
