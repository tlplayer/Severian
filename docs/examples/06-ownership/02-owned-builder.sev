class Server:
    host: string
    port: int


def server(host: string, port: int) -> Server:
    result := Server()
    result.host = host
    result.port = port
    return result


test:
    server := server("localhost", 8080)

    assert(server.host == "localhost")
    assert(server.port == 8080)