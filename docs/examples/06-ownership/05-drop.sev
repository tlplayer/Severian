class Resource:
    id: int

    def drop():
        print("released:", id)


def use_resource():
    resource := Resource(42)
    print(resource.id)


def main():
    use_resource()

test with compiler:
    resource := Resource(42)

    drop(resource)

    reject:
        print(resource.id)