class Buffer:
    bytes: list[u8]

    def push(byte: u8):
        bytes.append(byte)

def freeze(buffer: Buffer) -> Buffer:
    owned = buffer.move()
    return owned

def main():
    buffer := Buffer([])
    buffer.push(65)

    frozen = freeze(buffer)
    print(frozen.bytes)
