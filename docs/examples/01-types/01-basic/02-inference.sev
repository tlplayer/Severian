def main():
    count = 10
    ratio = 0.5
    enabled = true
    letter = 'l'
    name = "severian"

    print(f"{count}:{type(count)}")
    print(f"{ratio}:{type(ratio)}")
    print(f"{letter}:{type(letter)}")
    print(f"{enabled}:{type(enabled)}")
    print(f"{name}:{type(name)}")