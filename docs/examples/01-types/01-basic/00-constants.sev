MAX_RETRIES: int = 3
PI: float = 3.1415926

def main():
    print(MAX_RETRIES)
    print(PI)

test with integ "constant output":
    print(MAX_RETRIES)
    print(PI)
    assert("3" in stdout)
    assert("3.1415926" in stdout)
