
def main():
    #Init
    array = (1,2,3,4)
    #Index
    print(array[2])
    #Reverse tuple
    print(array[::-1])

test with integ "":
    main()
    assert("3" in stdout)
    assert("(4,3,2,1)" in stdout)