def divide(a: f64, b: f64) -> Result[f64, DivisionByZeroError]:
    if b == 0:
        return failure("division by zero")

    return ok(a / b)

def main():
    result ?= divide(10, 2)

    switch result:
        ok value:
            print(value)

        failure error:
            print(error)