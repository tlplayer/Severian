enum Color:
    Red
    Green
    Blue

def name(color: Color) -> string:
    match color:
        case Red:
            return "red"

        case Green:
            return "green"

        case Blue:
            return "blue"


test:
    assert(name(Red) == "red")
    assert(name(Blue) == "blue")
