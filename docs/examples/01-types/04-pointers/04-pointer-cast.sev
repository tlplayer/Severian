test:
    value: u32 := 42

    unsafe:
        integer_pointer: pointer[u32] := &value
        byte_pointer: pointer[u8] := [pointer[u8]](integer_pointer)

        #You don't need [0B] [] infers the pointed address without offset
        assert(byte_pointer[] == 42)