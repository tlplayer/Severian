test:
    value := 42

    unsafe:
        p: pointer[int] := &value

        # Indexing keeps raw access explicit; there is no unary dereference.
        assert(p[0] == 42)

        p[0] = 100

    assert(value == 100)
