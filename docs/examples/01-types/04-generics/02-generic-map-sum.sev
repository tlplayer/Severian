trait Number:
    zero() -> Self
    add(other: Self) -> Self

def sum_values[K: Hash + Equal, V: Number](Vs: map[K, V]) -> V:
    total := V.zero()

    for _, V in Vs:
        total = total.add(V)

    return total

def main():
    counts = {"first": 34, "second": 8}
    print(sum_values(counts))
