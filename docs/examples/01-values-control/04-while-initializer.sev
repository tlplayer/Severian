def main():
    while count < 3 with count := 0:
        print(count)
        count += 1

    done = true
    print(done)

test with integration "while output":
    main()
    assert("0" in stdout)
    assert("1" in stdout)
    assert("2" in stdout)
    assert("true" in stdout)
