def smooth(list[float] input) -> list[float]:
    output := []

    for i in range(1, size(input) - 1):
        value = (input[i - 1] + input[i] + input[i + 1]) / 3.0
        output.append(value)

    return output

def main():
    print(smooth([1.0, 2.0, 4.0, 8.0]))
