import math

def worker(id: int) -> int:
    return id * 2

@math(X)
def main():
    first = async worker(10)
    second = async worker(20)

    a = await first
    b = await second

    x = matrix.rand(1024, 1024)
    y = x X x

    print(a + b)
    print(y.shape)
