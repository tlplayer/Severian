import math
import probability

@math(X)
@probability(P)
def expectedProjection(weights: Matrix[f32], samples: Matrix[f32]) -> f32:
    projected = weights X samples
    return P(projected > 0.5)

def main():
    weights = matrix.rand(4, 4)
    samples = matrix.rand(4, 4)

    print(expectedProjection(weights, samples))
