class Buffer:
    bytes: list[u8]

    # [] is a mutable value by default
    def push(byte: u8):
        bytes.append(byte)

test:
    buffer = Buffer()
    buffer.push(65)
    assert(size(buffer.bytes) == 1)
