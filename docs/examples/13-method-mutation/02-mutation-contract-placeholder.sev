class Buffer:
    bytes: list[u8]

    # Placeholder: decide whether this needs a mutation marker.
    def push(byte: u8):
        bytes.append(byte)

test:
    buffer = Buffer([])
    buffer.push(65)
    assert(buffer.bytes.len() == 1)
