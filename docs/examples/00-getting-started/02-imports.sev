import file

def main():
    distance = 9.0 ** .5
    path = "/tmp/severian-getting-started-imports.txt"
    file.write(path, string(distance))
    file.read(path)
    file_distance = document.text()
    print(file_distance)


test with integration "imported output":
    main()
    assert("3" in stdout)
