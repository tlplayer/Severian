# Track: LeetCode 75
# Problem: 2390 - Removing Stars From a String
# Difficulty: medium
# Tags: string, stack

def removeStars(text: string) -> string:
    stack := []
    top := 0
    for index in range(0, size(text)):
        character = text[index]
        if character == "*":
            top -= 1
        else:
            if top < size(stack):
                stack[top] = character
            else:
                stack.append(character)
            top += 1

    result := ""
    for index in range(0, top):
        result += stack[index]
    return result

test "several removals":
    assert(removeStars("leet**cod*e") == "lecoe")

test "everything removed":
    assert(removeStars("erase*****") == "")

test "interleaved removals":
    assert(removeStars("abb*cdfg*****x*") == "a")
