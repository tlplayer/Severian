# Track: LeetCode 75
# Problem: 1732 - Find the Highest Altitude
# Difficulty: easy
# Tags: array, prefix-sum, math
# A cyclist starts at altitude zero. Return the highest altitude reached after
# applying each gain or loss in order.

def largestAltitude(gain: list[int]) -> int:
    altitude := 0
    highest := 0

    for change in gain:
        altitude += change
        if altitude > highest:
            highest = altitude

    return highest

test "mixed gains and losses":
    assert(largestAltitude([-5, 1, 5, 0, -7]) == 1)

test "steadily climbing":
    assert(largestAltitude([-4, -3, -2, -1, 4, 3, 2]) == 0)

test "empty route remains at sea level":
    assert(largestAltitude([]) == 0)
