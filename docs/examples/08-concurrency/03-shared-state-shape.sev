class Counter:
    value: int

    def inc():
        value += 1

def countAll(values: list[int]) -> int:
    counter := Counter(0)

    for value in values:
        if value > 0:
            counter.inc()

    return counter.value

def main():
    task = async countAll([1, -2, 3]) with self
    print(await task)
