def refreshCache():
    print("cache refreshed")

def main():
    # This task is intentionally owned by the runtime. Main does not wait for it,
    # so its work must own its inputs, return unit, and handle its own failures.
    unsafe:
        async refreshCache() with runtime
