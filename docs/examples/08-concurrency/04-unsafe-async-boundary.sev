def rawDriverCall() -> int:
    return 42

def main():
    # Runtime ownership is explicit even when the task is awaited here.
    unsafe:
        task = async rawDriverCall() with runtime

    print(await task)
