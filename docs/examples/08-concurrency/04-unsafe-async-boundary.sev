def raw_driver_call() -> int:
    return 42

def main():
    # Runtime ownership is explicit even when the task is awaited here.
    unsafe:
        task = async raw_driver_call() with runtime

    print(await task)
