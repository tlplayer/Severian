def raw_driver_call() -> int:
    return 42

def main():
    # This example explicitly demonstrates crossing into runtime ownership.
    unsafe:
        task = async raw_driver_call() with runtime

    print(await task)
