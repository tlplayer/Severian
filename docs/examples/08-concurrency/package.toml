[package]
name = "concurrency-examples"
version = "0.1.0"
edition = "2026"

[package.unsafe]
capabilities = ["runtime-owned-tasks"]
sources = [
    "04-unsafe-async-boundary.sev",
    "05-runtime-owned-background-work.sev",
]
