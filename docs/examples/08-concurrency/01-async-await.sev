def work(value: int) -> int:
    return value * 2

test:
    task = async work(21)
    assert(await task == 42)

def main():
    first = async work(10)
    second = async work(21)

    print(await first + await second)
