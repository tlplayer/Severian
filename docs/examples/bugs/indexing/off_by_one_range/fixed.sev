def lowerBound(values: list[int], target: int) -> int:
    low := 0
    high := size(values)

    while low < high:
        middle = low + (high - low) / 2
        if values[middle] < target:
            low = middle + 1
        else:
            high = middle

    return low

def main():
    values = [10, 20, 30]
    for i in indices(values):
        print(values[i])

    assert(lowerBound(values, 20) == 1)
    assert(lowerBound(values, 25) == 2)
