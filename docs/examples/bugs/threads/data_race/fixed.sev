class Counter:
    v: int = 0

    def increment():
        v += 1

def main():
    counter := Counter()
    left = async counter.increment() with self
    right = async counter.increment() with self

    await right, left
    assert(counter.v == 2)
