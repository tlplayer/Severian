from math import sqrt

class Point:
    x: float
    y: float

    def Point(x0: float, y0: float):
        x = x0
        y = y0

    def magnitude() -> float:
        return sqrt(x * x + y * y)

test:
    assert(Point(3.0, 4.0).magnitude() == 5.0)
