import c from xxi


@c(symbol = "strlen")
def c_strlen(text: borrowed[string]) -> usize


def string_length(text: string) -> usize:
    unsafe:
        return c_strlen(text)

def main():
    print(string_length("severian"))
