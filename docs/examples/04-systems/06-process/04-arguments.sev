import cli


def interface() -> cli.Command:
    return cli.command(
        "arguments",
        "Print each application argument",
        arguments=[
            cli.argument(
                "values",
                "Values to print",
                required=false,
                multiple=true,
            ),
        ],
    )


def main():
    application = interface()
    matches = cli.parse_process(application)
    for value in cli.values(matches, "values"):
        print(value)
