import file
import os


test with integ "file lock":
    path = "/tmp/severian-lock.txt"

    file.write(path, "shared state")

    lock = file.lock(path)

    assert(lock >= 0)

    file.unlock(lock)
    os.remove(path)
