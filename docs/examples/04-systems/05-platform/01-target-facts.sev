import platform


test "target facts are internally consistent":
    target = platform.current()

    assert(target.pointer_bits == 32b or target.pointer_bits == 64b)
    assert(target.byte_order == platform.LittleEndian or target.byte_order == platform.BigEndian)
    assert(target.architecture != "")
    assert(target.operating_system != "")
