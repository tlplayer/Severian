test:
    value := 42

    unsafe:
        pointer: *[int] := &value

        # Indexing keeps raw access explicit; there is no unary dereference.
        assert(pointer[0] == 42)

        pointer[0] = 100

    assert(value == 100)
