import environment as env


test "environment variable lifecycle":
    name = "SEVERIAN_EXAMPLE_VALUE"

    env.remove(name)

    assert(env.set(name, "hello"))
    assert(env.get(name) == "hello")

    assert(env.remove(name))
    assert(env.get(name, "missing") == "missing")
