import io


test "seek":
    stream = io.MemoryStream([1, 2, 3, 4])

    stream.seek(-1B, io.End)

    assert(stream.tell() == 3B)
    assert(stream.read(1B) == [4])

    stream.seek(0B, io.Start)

    assert(stream.read(2B) == [1, 2])
