import io


test "stream copy":
    source = io.MemoryStream([1, 2, 3, 4, 5])
    destination = io.MemoryStream([])

    copied = io.copy(source, destination, 2B)

    assert(copied == 5B)

    destination.seek(0B, io.Start)

    assert(io.read_all(destination) == [1, 2, 3, 4, 5])
