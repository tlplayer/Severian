# channel[T,N] elements
def main():
    messages = channel[string,10]

    producer = async messages.send("hello") with self
    producer = async messages.send("hello") with self
    message = await messages
    print(message)
    message = message.receive()

    print(message)
