import tensor

def portableForward(input: list[float]) -> list[float]:
    return tensor.parallelRelu(input)

def main():
    input = [-2.0, 3.0, -4.0, 5.0]
    print(portableForward(input))
    print(tensor.parallelRelu(input))
