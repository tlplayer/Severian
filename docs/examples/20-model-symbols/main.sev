import model

@model(Relu, FastSigmoid, J)
def main():
    x = [-2.0, 0.0, 3.0]
    values = Relu(x)
    centered = FastSigmoid([0.0])
    jacobian = J(x)

    print(values)
    print(centered)
    print(jacobian)
