# Executable database server

This example starts the real SQLite-backed loopback database server, connects a
client, executes DDL and inserts over TCP, queries rows, iterates them in
Severian, and shuts the server down cleanly.
