# Track: LeetCode 75
# Problem: 1456 - Maximum Number of Vowels in a Substring of Given Length
# Difficulty: medium
# Tags: string, sliding-window

def is_vowel(character: string) -> bool:
    return character == "a" or character == "e" or character == "i" or character == "o" or character == "u"

def max_vowels(text: string, width: int) -> int:
    vowels := 0
    for index in range(0, width):
        if is_vowel(text[index]):
            vowels += 1

    best := vowels
    for right in range(width, size(text)):
        if is_vowel(text[right]):
            vowels += 1
        if is_vowel(text[right - width]):
            vowels -= 1
        if vowels > best:
            best = vowels
    return best

test "representative window":
    assert(max_vowels("abciiidef", 3) == 3)

test "single vowel":
    assert(max_vowels("leetcode", 3) == 2)

test "no vowels":
    assert(max_vowels("rhythms", 4) == 0)
