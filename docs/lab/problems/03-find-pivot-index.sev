# Track: LeetCode 75
# Problem: 724 - Find Pivot Index
# Difficulty: easy
# Tags: array, prefix-sum
# Return the first index whose left-hand sum equals its right-hand sum, or -1
# when no such index exists.

def pivot_index(values: list[int]) -> int:
    total = values.sum()
    for index, value in enumerate(values) with left := 0:
        right = total - left - value
        if left == right:
            return index
        left += value

    return -1

test "pivot in the middle":
    assert(pivot_index([1, 7, 3, 6, 5, 6]) == 3)

test "no pivot":
    assert(pivot_index([1, 2, 3]) == -1)

test "first index is the pivot":
    assert(pivot_index([2, 1, -1]) == 0)

test "single value":
    assert(pivot_index([9]) == 0)
