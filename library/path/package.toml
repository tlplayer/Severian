[package]
name = "path"
version = "0.1.0"
edition = "2026"
description = "Filesystem path operations"

[lib]
path = "src/lib.sev"

[dependencies]
os = { path = "../os", version = "0.1.0" }
platform = { path = "../platform", version = "0.1.0" }
