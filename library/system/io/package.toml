[package]
name = "io"
version = "0.1.0"
edition = "2026"
description = "Portable streams, standard handles, and printing"

[package.unsafe]
capabilities = ["native-abi"]
sources = ["src/lib.sev"]

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "system"
implementation = "native-abi"
status = "experimental"
version = 1
