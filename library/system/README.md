# system

The `system` package is Severian's process and interpreter-facing namespace.
The complete word is intentional; Severian does not expose a clipped `sys`
alias. Its interface remains empty until the corresponding runtime behavior is
implemented rather than presenting unsupported placeholders.
