[package]
name = "system"
version = "0.1.0"
edition = "2026"
description = "Process and interpreter-facing system information"

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "time-environment"
implementation = "interface-pending"
status = "planned"
