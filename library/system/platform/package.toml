[package]
name = "platform"
version = "0.1.0"
edition = "2026"
description = "Selected target facts, capabilities, and data layout"

[package.unsafe]
capabilities = ["native-abi"]
sources = ["src/lib.sev"]

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "system"
implementation = "native-abi"
status = "experimental"
version = 1
