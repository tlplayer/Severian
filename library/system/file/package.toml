[package]
name = "file"
version = "0.1.0"
edition = "2026"
description = "Typed file access"

[package.unsafe]
capabilities = ["native-abi"]
sources = ["src/lib.sev"]

[lib]
path = "src/lib.sev"

[dependencies]
data = { path = "../../data", version = "0.1.0" }
