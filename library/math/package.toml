[package]
name = "math"
version = "0.1.0"
edition = "2026"
description = "Portable mathematics and compiler-backed numeric intrinsics"

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "numerics"
implementation = "source-and-intrinsics"
status = "experimental"

