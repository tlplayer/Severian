[package]
name = "model.codecs.higgs_audio_v2"
version = "0.1.0"
edition = "2026"
description = "Higgs Audio V2 neural audio codec"

[lib]
path = "src/lib.sev"

[dependencies]
audio = { path = "../../../audio", version = "0.1.0" }

[package.metadata.library]
category = "machine-learning"
implementation = "source"
status = "scaffold"

