import json

def inferred_types(rows: list[list[Any]], width: int) -> list[string]:
    result := []
    for position in range(width):
        kind := "null"
        for values in rows:
            value_kind = json.kind(values[position]) if position < size(values) else "null"
            if value_kind != "null":
                if kind == "null":
                    kind = value_kind
                else:
                    if kind != value_kind:
                        kind = "any"
        result.append(kind)
    return result

def inferred_nullability(rows: list[list[Any]], width: int) -> list[bool]:
    result := []
    for position in range(width):
        nullable := false
        for values in rows:
            if position >= size(values) or json.kind(values[position]) == "null":
                nullable = true
        result.append(nullable)
    return result
