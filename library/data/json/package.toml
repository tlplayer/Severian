[package]
name = "json"
version = "0.1.0"
edition = "2026"
description = "JSON documents, typed codecs, and file-format integ"

[lib]
path = "src/lib.sev"

[package.unsafe]
capabilities = ["native-abi"]
sources = ["src/lib.sev"]

[dependencies]
data = { path = "..", version = "0.1.0" }
file = { path = "../../system/file", version = "0.1.0" }

[package.metadata.library]
category = "data-formats"
implementation = "source-and-native"
status = "experimental"
