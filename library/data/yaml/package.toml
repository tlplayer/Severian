[package]
name = "yaml"
version = "0.1.0"
edition = "2026"
description = "YAML documents and mapping access"

[lib]
path = "src/lib.sev"

[dependencies]
data_format = { path = "../format", version = "0.1.0" }
json = { path = "../json", version = "0.1.0" }
file = { path = "../../system/file", version = "0.1.0" }

[package.metadata.library]
category = "data-formats"
implementation = "source"
status = "experimental"
