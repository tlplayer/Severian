[package]
name = "audio"
version = "0.1.0"
edition = "2026"
description = "Typed audio buffers, WAV I/O, and sample-rate conversion"

[lib]
path = "src/lib.sev"

[dependencies]
file = { path = "../file", version = "0.1.0" }
tensor = { path = "../tensor", version = "0.1.0" }

[package.metadata.library]
category = "media"
implementation = "source"
status = "experimental"

