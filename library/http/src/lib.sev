import network
import platform

class Request:
    method: string
    path: string
    body: string

class Response:
    status: int = 200
    body: string = ""

class Server:
    address: string

def request(method: string, url: string, body: string = "") -> string | IOError:
    return platform.http_request(method, url, body)

def get(url: string) -> string | IOError:
    return request("GET", url)

def post(url: string, body: string = "") -> string | IOError:
    return request("POST", url, body)

def put(url: string, body: string = "") -> string | IOError:
    return request("PUT", url, body)

def delete(url: string) -> string | IOError:
    return request("DELETE", url)

def download(url: string, destination: string) -> unit | IOError:
    return platform.http_download(url, destination)

test "HTTP verb helpers preserve typed failures for invalid URLs":
    match request("PATCH", "invalid"):
        case _response:
            assert(false, "an invalid URL unexpectedly succeeded")
        failure _error:
            assert(true)
    match get("invalid"):
        case _response:
            assert(false, "an invalid URL unexpectedly succeeded")
        failure _error:
            assert(true)
    match post("invalid", "payload"):
        case _response:
            assert(false, "an invalid URL unexpectedly succeeded")
        failure _error:
            assert(true)
    match put("invalid", "payload"):
        case _response:
            assert(false, "an invalid URL unexpectedly succeeded")
        failure _error:
            assert(true)
    match delete("invalid"):
        case _response:
            assert(false, "an invalid URL unexpectedly succeeded")
        failure _error:
            assert(true)

test "HTTP message types retain constructor values and defaults":
    request_value = Request("POST", "/events", "payload")
    response_value = Response()
    server = Server("127.0.0.1:8080")
    assert(request_value.method == "POST")
    assert(request_value.path == "/events")
    assert(request_value.body == "payload")
    assert(response_value.status == 200)
    assert(response_value.body.is_empty())
    assert(server.address == "127.0.0.1:8080")
