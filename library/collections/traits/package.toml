[package]
name = "collections.traits"
version = "0.1.0"
edition = "2026"
description = "Minimal capability traits shared by collection packages"

[lib]
path = "src/lib.sev"

[dependencies]
primitives = { path = "../../core/primitives", version = "0.1.0" }

[package.metadata.library]
category = "core-data"
implementation = "source"
status = "experimental"
