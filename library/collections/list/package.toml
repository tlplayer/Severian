[package]
name = "list"
version = "0.1.0"
edition = "2026"
description = "Source-owned generic contiguous sequences"

[lib]
path = "src/lib.sev"

[dependencies]
traits = { path = "../traits", version = "0.1.0" }
storage = { path = "../internal/contiguous_storage", version = "0.1.0" }

[package.metadata.library]
category = "core-data"
implementation = "source"
status = "experimental"
