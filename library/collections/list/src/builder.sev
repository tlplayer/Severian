import "list.sev"

class ListBuilder[T]:
    values: list[T]

    def push(value: T):
        values.append(value)

    def finish() -> list[T]:
        return move values


def builder[T](capacity: usize) -> ListBuilder[T]:
    return ListBuilder[T](with_capacity[T](capacity))
