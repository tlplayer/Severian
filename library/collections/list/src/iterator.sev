import traits
import "list.sev"

class ListIterator[T]: traits.Iterator[T]
    values: borrow list[T]
    index: usize

    def next() -> T | None:
        if index >= values.len():
            return None
        value = values.get(index)
        index += 1
        return value
