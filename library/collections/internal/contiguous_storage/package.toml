[package]
name = "collections.internal.contiguous_storage"
version = "0.1.0"
edition = "2026"
description = "Private move-aware contiguous storage for source-owned collections"
publish = false

[package.unsafe]
capabilities = ["pointers", "unsafe-blocks"]
sources = ["src/storage.sev"]

[lib]
path = "src/lib.sev"

[dependencies]
primitives = { path = "../../../core/primitives", version = "0.1.0" }

[package.metadata.library]
category = "internal"
implementation = "source"
status = "experimental"
public = false
