import platform

def info(message: string):
    platform.logInfo(message)

def error(message: string, cause: Error):
    platform.logError(message, cause)

test "native logging sinks":
    info("library ready")
    error("library failure", IOError("test cause"))
