[package]
name = "log"
version = "0.1.0"
edition = "2026"
description = "Structured application logging"

[lib]
path = "src/lib.sev"

[dependencies]
platform = { path = "../platform", version = "0.1.0" }

[package.metadata.library]
category = "observability"
implementation = "source-and-runtime"
status = "experimental"
