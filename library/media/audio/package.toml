[package]
name = "audio"
version = "0.1.0"
edition = "2026"
description = "Typed audio buffers, WAV I/O, and sample-rate conversion"

[lib]
path = "src/lib.sev"

[dependencies]
file = { path = "../../system/file", version = "0.1.0" }
math = { path = "../../core/math", version = "0.1.0" }
tensor = { path = "../../compute/tensor", version = "0.1.0" }

[package.metadata.library]
category = "media"
implementation = "source"
status = "experimental"
