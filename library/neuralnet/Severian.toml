[package]
name = "neuralnet"
version = "0.1.0"
edition = "2026"
description = "Neural-network layers and activation derivatives built on tensor"

[lib]
path = "src/lib.sev"

[dependencies]
tensor = { path = "../tensor", version = "0.1.0" }
distributed = { path = "../distributed", version = "0.1.0" }

[package.metadata.library]
category = "machine-learning"
implementation = "source"
status = "experimental"
