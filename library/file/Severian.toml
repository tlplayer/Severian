[package]
name = "file"
version = "0.1.0"
edition = "2026"
description = "File operations and owned file handles"

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "files-io"
implementation = "runtime"
status = "runtime-pending"
