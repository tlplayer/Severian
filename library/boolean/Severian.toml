[package]
name = "boolean"
version = "0.1.0"
edition = "2026"
description = "Boolean aggregation and predicates"

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "core-data"
implementation = "source"
status = "experimental"

