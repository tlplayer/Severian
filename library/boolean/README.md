# boolean

Boolean aggregation helpers implemented in Severian source.

- `all(values)` is true when every value is true, including an empty list.
- `any(values)` is true when at least one value is true.
- `none(values)` is true when no value is true.

