[package]
name = "probability"
version = "0.1.0"
edition = "2026"
description = "Probability values and compiler symbol contracts"

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "numerics"
implementation = "source"
status = "experimental"

[package.metadata.compiler]
symbols = { P = "probability" }
