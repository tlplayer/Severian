[package]
name = "string"
version = "0.1.0"
edition = "2026"
description = "Unicode string operations"

[lib]
path = "src/lib.sev"

[dependencies]
platform = { path = "../platform", version = "0.1.0" }
