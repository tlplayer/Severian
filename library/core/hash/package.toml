[package]
name = "hash"
version = "0.1.0"
edition = "2026"
description = "Deterministic non-secret hashing"

[lib]
path = "src/lib.sev"

[dependencies]
platform = { path = "../platform", version = "0.1.0" }

[package.metadata.library]
category = "security"
implementation = "runtime"
status = "experimental"
