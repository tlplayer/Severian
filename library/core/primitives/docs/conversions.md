# Conversions

Conversions are explicit unless the semantic compatibility rules can prove a
lossless conversion from category, signedness, and width. Conversion policy is
owned by semantic type compatibility; lowering only emits an already-resolved
conversion.
