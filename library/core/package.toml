[package]
name = "core"
version = "0.1.0"
edition = "2026"
description = "Core collection and scalar functions"

[lib]
path = "src/lib.sev"

[dependencies]
platform = { path = "../platform", version = "0.1.0" }
