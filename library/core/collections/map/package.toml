[package]
name = "map"
version = "0.1.0"
edition = "2026"
description = "Generic key-value maps"

[lib]
path = "src/lib.sev"

[dependencies]
table = { package = "core.collections.internal.hash_table", path = "../internal/hash_table", version = "0.1.0" }
traits = { package = "collections.traits", path = "../traits", version = "0.1.0" }

[package.metadata.library]
category = "core-collections"
implementation = "source"
status = "experimental"
