[package]
name = "set"
version = "0.1.0"
edition = "2026"
description = "Generic unique-value sets"

[lib]
path = "src/lib.sev"

[dependencies]
table = { path = "../internal/hash_table", version = "0.1.0" }
traits = { path = "../traits", version = "0.1.0" }

[package.metadata.library]
category = "core-collections"
implementation = "source"
status = "experimental"
