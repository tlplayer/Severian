[package]
name = "list"
version = "0.1.0"
edition = "2026"
description = "Source-owned generic contiguous lists"

[lib]
path = "src/lib.sev"

[dependencies]
storage = { path = "../internal/contiguous_storage", version = "0.1.0" }

[package.metadata.library]
category = "core-collections"
implementation = "source"
status = "experimental"
