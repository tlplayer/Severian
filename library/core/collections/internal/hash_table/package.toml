[package]
name = "core.collections.internal.hash_table"
version = "0.1.0"
edition = "2026"
description = "Private associative storage shared by core set and map"
publish = false

[lib]
path = "src/lib.sev"

[dependencies]
storage = { path = "../contiguous_storage", version = "0.1.0" }
traits = { path = "../../traits", version = "0.1.0" }

[package.metadata.library]
category = "internal"
implementation = "source"
status = "experimental"
public = false
