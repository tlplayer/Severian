[package]
name = "queue"
version = "0.1.0"
edition = "2026"
description = "Generic first-in-first-out queues"

[lib]
path = "src/lib.sev"

[dependencies]
deque = { path = "../deque", version = "0.1.0" }

[package.metadata.library]
category = "core-collections"
implementation = "source"
status = "experimental"
