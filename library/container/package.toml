[package]
name = "container"
version = "0.1.0"
edition = "2026"
description = "Container image, process, namespace, cgroup, network, and storage plans"

[lib]
path = "src/lib.sev"

[dependencies]
platform = { path = "../platform", version = "0.1.0" }

[package.metadata.library]
category = "infrastructure"
implementation = "source-and-native"
status = "experimental"
