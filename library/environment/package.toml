[package]
name = "environment"
version = "0.1.0"
edition = "2026"
description = "Process environment access"

[lib]
path = "src/lib.sev"

[dependencies]
platform = { path = "../platform", version = "0.1.0" }
