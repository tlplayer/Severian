[package]
name = "io"
version = "0.1.0"
edition = "2026"
description = "Standard input, output, and stream interfaces"

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "files-io"
implementation = "source-and-runtime"
status = "runtime-pending"

