[package]
name = "matrix"
version = "0.1.0"
edition = "2026"
description = "Symbolic linear algebra and compiler-backed matrix primitives"

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "numerics"
implementation = "source-and-intrinsics"
status = "experimental"
