[package]
name = "regex"
version = "0.1.0"
edition = "2026"
description = "Regular-expression compilation and matching"

[lib]
path = "src/lib.sev"

[package.metadata.library]
category = "text-processing"
implementation = "compiler-symbol-pack"
status = "interface-pending"

