[package]
name = "model.layers"
version = "0.1.0"
edition = "2026"
description = "Generic tensor-native neural-network layers"

[lib]
path = "src/lib.sev"

[dependencies]
tensor = { path = "../../tensor", version = "0.1.0" }

[package.metadata.library]
category = "machine-learning"
implementation = "source"
status = "experimental"
