[package]
name = "model.architectures.omnivoice"
version = "0.1.0"
edition = "2026"
description = "Composed OmniVoice model plan"

[lib]
path = "src/lib.sev"

[dependencies]
audio = { path = "../../../audio", version = "0.1.0" }

[package.metadata.library]
category = "machine-learning"
implementation = "source"
status = "scaffold"

