[package]
name = "safetensor_weights"
version = "0.1.0"
edition = "2026"

[lib]
path = "../../safetensor_weights.sev"

[dependencies]
file = { path = "../../../../../library/file", version = "0.1.0" }
json = { path = "../../../../../library/json", version = "0.1.0" }
tensor = { path = "../../../../../library/tensor", version = "0.1.0" }
