import platform

def encode(modelPath: string, text: string) -> list[int]:
    return platform.qwen_tokenize(modelPath, text)

def decodeToken(modelPath: string, token: int) -> string:
    return platform.qwen_decode_token(modelPath, token)
