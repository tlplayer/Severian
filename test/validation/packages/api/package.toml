[package]
name = "package-api-tests"
version = "0.1.0"
edition = "2026"

[lib]
path = "main.sev"

[dependencies]
package = { path = "../../../../library/package" }
environment = { path = "../../../../library/system/environment" }
