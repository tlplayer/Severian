[package]
name = "examples-validation"
version = "0.1.0"
edition = "2026"
publish = false

[lib]
path = "src/validation.sev"

[dependencies]
core = { path = "../../../library/core" }
collections = { path = "../../../library/collections" }
testing = { path = "../../../library/testing" }
compute = { path = "../../../library/compute" }
system = { path = "../../../library/system" }
tensor = { path = "../../../library/tensor" }

[build]
backend = "auto"

