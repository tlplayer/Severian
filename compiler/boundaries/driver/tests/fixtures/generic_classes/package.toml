[package]
name = "generic_classes"
version = "0.1.0"
edition = "2026"

[lib]
path = "linear.sev"

[dependencies]
layers = { package = "model.layers", path = "../../../../../library/model/layers", version = "0.1.0" }
tensor = { path = "../../../../../library/tensor", version = "0.1.0" }
